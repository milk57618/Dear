<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eunsol.dear">


      <!--WIFI 상태활용시 추가--><uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
      <!--WIFI 상태활용시 추가--><uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
      <!--반드시 추가--><uses-permission android:name="android.permission.INTERNET" />
      <!--블루투스 권한--><uses-permission android:name="android.permission.BLUETOOTH"/>
                         <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>

    <application
        android:name=".main.CustomFontApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >

        <activity android:name=".main.Activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".main.Activity.SaleActivity">
        </activity>

        <activity android:name=".main.Activity.MemberActivity">
        </activity>

        <activity android:name=".main.Activity.OrnerActivity">
        </activity>

        <activity android:name=".main.Activity.AnalysisActivity">
        </activity>

        <activity android:name=".main.Activity.ClientActivity">
        </activity>

        <activity android:name=".main.Activity.MenuActivity">
        </activity>

        <activity android:name=".main.Activity.OrderListActivity">
        </activity>

    </application>

</manifest>